spring:
  cloud:
    config:
      label: master
      discovery:
        enabled:  true
        serviceId:  core-server-config
  client:
    serviceUrl:
      defaultZone:  http://localhost:10101/eureka/